[pytest]
markers =
    live: hits real paid providers — opt in with `pytest -m live`
# Live tests are excluded by default: a clip costs minutes and dollars, so they
# must be asked for. Everything else runs offline in seconds.
addopts = -m "not live"
